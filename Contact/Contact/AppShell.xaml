<Shell xmlns="http://xamarin.com/schemas/2014/forms"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:local="clr-namespace:Contact.Views"
       Title="Contact"
       x:Class="Contact.AppShell"
       FlyoutBehavior="Disabled">

    <Shell.Resources>
        <ResourceDictionary>
        </ResourceDictionary>
    </Shell.Resources>

    <!-- Affichez directement la page ItemsPage -->
    <ShellContent Route="ItemsPage" ContentTemplate="{DataTemplate local:ContactPage}" />
    <ShellContent Route="ContactDetailPage" ContentTemplate="{DataTemplate local:ContactDetailPage}" />
    <ShellContent Route="NewContactPage" ContentTemplate="{DataTemplate local:NewContactPage}" />
</Shell>